// Отображение удаленных репозиториев
git remote

// Отображение удаленных репозиториев.
// Параметр -v позволяет увидеть URL-адреса, которые Git хранит для сокращенного имени,
// используемого при чтении из данного удаленного репозитория и при записи в него:
git remote -v

// Пуш изменений с установкой дефолтного репозиторий и создания ветки в этом репозитории
git push --set-upstream nikolai1 master

// Удаление удаленного репозитория 
git remote remove origin 